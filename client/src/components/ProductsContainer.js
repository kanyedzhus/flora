import React, { useContext } from "react";

export default function ProductsContainer() {
	// const { products } = useContext(ProductsContext);

	return (
		<div class="container">
			<div class="row row-cols-1 row-cols-sm-2 g-3"></div>
		</div>
	);
}
